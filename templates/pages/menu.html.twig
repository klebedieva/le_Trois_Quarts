{% extends 'base.html.twig' %}

{% block title %}Menu - Le Trois Quarts{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('assets/css/menu.css') }}?v={{ 'now'|date('YmdHis') }}">
{% endblock %}

{% block body %}
    {# Section héro comme dans Restaurant #}
    <section class="menu-hero py-5">
        <div class="container">
            <div class="row">
                <div class="col-lg-8 mx-auto text-center">
                    <h1 class="display-4 mb-4">Notre Carte</h1>
                    <p class="lead">Une cuisine généreuse alliant tradition et créativité</p>
                    <div class="menu-hero-divider"></div>
                </div>
            </div>
        </div>
    </section>

    {# Filtres du menu (structure identique Restaurant) #}
    <section class="menu-filters-section py-3">
        <div class="container">
            <div class="menu-filters">
                <div class="filter-categories">
                    <button class="filter-category active" data-category="all"><span class="emoji">🍽️</span><span>Tout</span></button>
                    <button class="filter-category" data-category="entrees"><span class="emoji">🥗</span><span>Entrées</span></button>
                    <button class="filter-category" data-category="plats"><span class="emoji">🍖</span><span>Plats</span></button>
                    <button class="filter-category" data-category="desserts"><span class="emoji">🍰</span><span>Desserts</span></button>
                    <button class="filter-category" data-category="boissons"><span class="emoji">🍷</span><span>Boissons</span></button>
                </div>

                <div class="search-filter-row">
                    <div class="search-price-row">
                        <div class="menu-search">
                            <i class="bi bi-search search-icon"></i>
                            <input type="text" class="form-control" id="menuSearch" placeholder="Rechercher un plat...">
                        </div>
                        <div class="price-filter">
                            <select class="form-select" id="priceFilter">
                                <option value="">Tous les prix</option>
                                <option value="under-15">Moins de 15€</option>
                                <option value="15-25">15€ - 25€</option>
                                <option value="over-25">Plus de 25€</option>
                            </select>
                        </div>
                    </div>

                    <div class="dietary-filters">
                        <div class="dietary-filter-item">
                            <input type="checkbox" id="vegetarian" class="dietary-filter">
                            <label for="vegetarian">🌱 Végétarien</label>
                        </div>
                        <div class="dietary-filter-item">
                            <input type="checkbox" id="vegan" class="dietary-filter">
                            <label for="vegan">🌿 Végan</label>
                        </div>
                        <div class="dietary-filter-item">
                            <input type="checkbox" id="glutenFree" class="dietary-filter">
                            <label for="glutenFree">🌾 Sans gluten</label>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>

    {# Contenu du menu #}
    <section class="menu-content py-5">
        <div class="container">
            <div id="menuGrid"><!-- rempli par JS --></div>
            <div id="noResults" class="no-results d-none">
                <i class="bi bi-search"></i>
                <h3>Aucun résultat trouvé</h3>
                <p>Essayez de modifier vos critères de recherche</p>
            </div>
        </div>
    </section>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        // Exposer les données du back au script front
        window.menuItems = {{ menuItemsJson|default('[]')|raw }};
        window.drinksData = {{ drinksJson|default('{}')|raw }};
    </script>
    <script src="{{ asset('assets/js/menu.js') }}"></script>
{% endblock %}


