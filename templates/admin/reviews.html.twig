<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Administration - Avis</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css">
</head>
<body>
    <div class="container-fluid">
        <div class="row">
            <div class="col-12">
                <div class="d-flex justify-content-between align-items-center py-4">
                    <h1><i class="bi bi-chat-dots me-2"></i>Gestion des avis</h1>
                    <a href="{{ path('app_home') }}" class="btn btn-outline-primary">
                        <i class="bi bi-arrow-left me-2"></i>Retour au site
                    </a>
                </div>

                {% for message in app.flashes('success') %}
                    <div class="alert alert-success alert-dismissible fade show" role="alert">
                        <i class="bi bi-check-circle me-2"></i>{{ message }}
                        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                    </div>
                {% endfor %}

                {% if reviews is empty %}
                    <div class="text-center py-5">
                        <i class="bi bi-chat-dots icon-large"></i>
                        <h4 class="mt-3">Aucun avis trouvé</h4>
                        <p class="text-muted">Aucun avis n'a été soumis pour le moment.</p>
                    </div>
                {% else %}
                    <div class="row">
                        {% for review in reviews %}
                            <div class="col-lg-6 mb-4">
                                <div class="card">
                                    <div class="card-header d-flex justify-content-between align-items-center">
                                        <div>
                                            <strong>{{ review.name }}</strong>
                                            <small class="text-muted ms-2">{{ review.createdAt|date('d/m/Y à H:i') }}</small>
                                        </div>
                                        <div>
                                            {% if review.isApproved %}
                                                <span class="badge bg-success">Approuvé</span>
                                            {% else %}
                                                <span class="badge bg-warning">En attente</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                    <div class="card-body">
                                        <div class="mb-3">
                                            {% for i in 1..5 %}
                                                {% if i <= review.rating %}
                                                    <i class="bi bi-star-fill text-warning"></i>
                                                {% else %}
                                                    <i class="bi bi-star text-warning"></i>
                                                {% endif %}
                                            {% endfor %}
                                            <span class="ms-2 fw-bold">{{ review.rating }}/5</span>
                                        </div>
                                        
                                        <p class="card-text">"{{ review.comment }}"</p>
                                        
                                        {% if review.email %}
                                            <p class="text-muted small mb-0">
                                                <i class="bi bi-envelope me-1"></i>{{ review.email }}
                                            </p>
                                        {% endif %}
                                    </div>
                                    <div class="card-footer">
                                        <div class="btn-group" role="group">
                                            {% if not review.isApproved %}
                                                <form method="post" action="{{ path('app_admin_review_approve', {'id': review.id}) }}" class="d-inline">
                                                    <button type="submit" class="btn btn-success btn-sm">
                                                        <i class="bi bi-check-circle me-1"></i>Approuver
                                                    </button>
                                                </form>
                                            {% else %}
                                                <form method="post" action="{{ path('app_admin_review_reject', {'id': review.id}) }}" class="d-inline">
                                                    <button type="submit" class="btn btn-warning btn-sm">
                                                        <i class="bi bi-x-circle me-1"></i>Rejeter
                                                    </button>
                                                </form>
                                            {% endif %}
                                            
                                            <form method="post" action="{{ path('app_admin_review_delete', {'id': review.id}) }}" class="d-inline" 
                                                  onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet avis ?')">
                                                <button type="submit" class="btn btn-danger btn-sm">
                                                    <i class="bi bi-trash me-1"></i>Supprimer
                                                </button>
                                            </form>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
